packages:
  - "apps/*"
  - "packages/*"
  - "services/*"
allowBuilds:
  # Prebuilt binaries ship with the package; skip the node-gyp fallback build.
  '@parcel/watcher': false
  esbuild: true
  sharp: true
